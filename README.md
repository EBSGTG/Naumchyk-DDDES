# Лабораторна робота №1

### Проектування систем з розподіленими базами даних в енергетиці

**Тема:** Дослідження продуктивності Apache Kafka для енергетичних
IoT-потоків\
**Варіант:** 6 --- Теплові електростанції Східної України\
**Підваріант:** A (оптимізація для real-time критичних алертів)

------------------------------------------------------------------------

## 🎯 Мета роботи

Дослідити продуктивність Apache Kafka для потоків даних з теплових
електростанцій.\
Фокус дослідження:\
- критичні real-time алерти\
- аналіз 50th та 95th percentile latency\
- вплив параметра `linger.ms` (1,2,3,5 ms)\
- моніторинг consumer lag та network jitter

------------------------------------------------------------------------

## ⚙️ Тестове середовище

-   **OS:** Ubuntu 24.04\
-   **Java:** 11\
-   **Kafka:** 3.7.1 (single-broker), Zookeeper 3.8\
-   **Python:** 3.12\
-   **Hardware:** 4 vCores, 4 GB RAM, SSD

### Тестові дані

-   Тип: дані з теплових електростанцій Східної України\
-   Формат: JSON, 256 байт\
-   Кількість: \~1100 згенерованих записів\
-   Ключові параметри: `fuel_flow`, `steam_pressure`, `power_output`,
    `efficiency`, `temperature`, `voltage`, `current`, `status`,
    `fuel_type`

------------------------------------------------------------------------

## 📊 Основні результати

### Batch size та linger.ms

  Конфігурація   Records/sec   Avg Latency (ms)   Використання
  -------------- ------------- ------------------ -----------------
  8KB, 0ms       650           4.5                Ultra real-time
  16KB, 0ms      900           6.0                Real-time
  64KB, 10ms     1500          12                 Balanced
  256KB, 50ms    2200          65                 Batch

🔹 **Оптимум:** `64KB + 5ms → ~1500 rec/sec при latency ~12ms`

### Compression

  ---------------------------------------------------------------------------
  Алгоритм   Records/sec   Latency (ms) Compression Ratio Використання
  ---------- ------------- ------------ ----------------- -------------------
  none       2100          5            0%                real-time алерти

  snappy     1900          8            50%               load regulation

  lz4        1750          12           60%               fuel optimization

  zstd       1500          18           70%               архівація
  ---------------------------------------------------------------------------

🔹 **Баланс:** LZ4 (60% компресія, latency \<15ms)

### Масштабування (партиції)

  Партиції   Records/sec   Scaling Factor   Ефективність
  ---------- ------------- ---------------- --------------------
  4          1000          1.0x             Baseline
  8          1800          1.8x             Good scaling
  12         2500          2.5x             Near-linear growth

🔹 **Оптимум:** 8 партицій для стабільного real-time + архівації

------------------------------------------------------------------------

## 🛠 Рекомендації для використання

-   **Real-time алерти**

        batch.size=16KB, linger.ms=0, compression=none, partitions=4

    Latency \<5ms \| Throughput \>900 rec/sec

-   **Batch-аналітика**

        batch.size=64KB, linger.ms=10, compression=snappy, partitions=8

    Економія \~40% \| Throughput \>1800 rec/sec

-   **Архівування**

        batch.size=128KB, linger.ms=50, compression=zstd, partitions=12

    Економія \~70% \| мінімум дискового простору

------------------------------------------------------------------------

## 📌 Висновки

-   **Latency ↔ Throughput trade-off** підтверджено\
-   **Zstd** дає найкращу компресію (70%), **LZ4** --- оптимальний
    баланс\
-   **Scaling** з партиціями близький до лінійного

➡️ **Рекомендована конфігурація:**\
`64KB + 5ms + LZ4 + 8 партицій → ~1500 rec/sec при latency ~12ms`

Практичне значення:\
- SCADA інтеграція для моніторингу fuel_flow у реальному часі\
- Архівування даних для environmental reporting\
- Балансування навантаження між блоками ТЕС за fuel_type

------------------------------------------------------------------------

## 📂 Додатки

### A. Генератор даних (Python)

``` python
import random, json
N = 12

def generate_powerplant_data():
    return {
        "device_id": f"TESC_{random.randint(1, N):03d}",
        "fuel_flow": round(random.uniform(120.0, 250.0), 2),
        "steam_pressure": round(random.uniform(14.0, 23.0), 2),
        "temperature": round(random.uniform(480.0, 560.0), 1),
        "timestamp": "2025-09-27T12:00:00Z"
    }

for _ in range(3):
    print(json.dumps(generate_powerplant_data(), ensure_ascii=False))
```

### B. Тестові команди Kafka

``` bash
kafka-producer-perf-test.sh   --topic test_powerplant   --num-records 100000   --record-size 256   --throughput -1   --producer-props bootstrap.servers=localhost:9092 batch.size=16384 linger.ms=0 compression.type=none
```

(інші команди для snappy та zstd наведені в звіті)

------------------------------------------------------------------------

✍️ **Автор:** Наумчик Є.Л., гр. ТР-52мп\
👨‍🏫 **Викладач:** Волков О.В.\
📍 КПІ ім. Ігоря Сікорського, 2025


# Лабораторна робота №2. Основи Cassandra

## Варіант 6: Теплові електростанції Східної України
## Підваріант А: Аналітичний

### 📋 Опис завдання

Ця лабораторна робота демонструє принципи проєктування розподілених схем даних у Apache Cassandra на прикладі енергетичних систем моніторингу. Створено систему моніторингу для 12 енергоблоків теплових електростанцій Східної України.

### 🏭 Модельовані станції

1. **Курахівська ТЕС** - 4 блоки, паливо: вугілля
2. **Вуглегірська ТЕС** - 3 блоки, паливо: вугілля  
3. **Слов'янська ТЕС** - 3 блоки, паливо: газ
4. **Зміївська ТЕС** - 2 блоки, паливо: вугілля

### 🗄️ Структура бази даних

#### Keyspace: `thermal_power_eastern_ukraine`

#### Таблиці:

1. **`boilers_data`** - Дані роботи котлів
   - `block_id` (UUID) - Partition Key
   - `timestamp` (TIMESTAMP) - Clustering Key (DESC)
   - `pressure` (DECIMAL) - Тиск пари, бар
   - `temperature` (DECIMAL) - Температура пари, °C
   - `fuel_consumption` (DECIMAL) - Витрата палива, т/год
   - `efficiency` (DECIMAL) - Коефіцієнт ефективності
   - `fuel_type` (TEXT) - Тип палива

2. **`turbines_data`** - Дані роботи турбін
   - `block_id` (UUID) - Partition Key
   - `timestamp` (TIMESTAMP) - Clustering Key (DESC)
   - `rpm` (DECIMAL) - Швидкість обертання, об/хв
   - `power_output` (DECIMAL) - Вироблена потужність, МВт
   - `load_percentage` (DECIMAL) - Навантаження, %
   - `vibration` (DECIMAL) - Вібрація, мм/с
   - `steam_pressure` (DECIMAL) - Тиск пари, бар

3. **`emissions_data`** - Викиди забруднюючих речовин
   - `block_id` (UUID) - Partition Key
   - `timestamp` (TIMESTAMP) - Clustering Key (DESC)
   - `co2_emissions` (DECIMAL) - Викиди CO₂, кг/МВт·год
   - `nox_emissions` (DECIMAL) - Викиди NOx, мг/м³
   - `so2_emissions` (DECIMAL) - Викиди SO₂, мг/м³
   - `particulate_matter` (DECIMAL) - Частки, мг/м³
   - `fuel_type` (TEXT) - Тип палива
   - `efficiency_factor` (DECIMAL) - Фактор ефективності

4. **`environmental_summary`** - Зведені екологічні показники
   - `station_name` (TEXT) - Partition Key
   - `date` (DATE) - Clustering Key (DESC)
   - `total_co2` (DECIMAL) - Загальні викиди CO₂, т/добу
   - `total_nox` (DECIMAL) - Загальні викиди NOx, кг/добу
   - `total_so2` (DECIMAL) - Загальні викиди SO₂, кг/добу
   - `avg_efficiency` (DECIMAL) - Середня ефективність
   - `total_power_generated` (DECIMAL) - Загальна генерація, МВт·год
   - `blocks_count` (INT) - Кількість блоків

### 🔧 Встановлення та запуск

#### Варіант 1: Docker Compose (Рекомендований)

**Передумови:**
- Docker Desktop
- Docker Compose

**Запуск:**
```bash
# Windows
docker-run.bat

# Linux/macOS
chmod +x docker-run.sh
./docker-run.sh

# Або вручну
docker-compose up --build
```

#### Варіант 2: Локальне встановлення (застарілий)

> ⚠️ **Увага:** Для локального встановлення потрібно окремо встановити Apache Cassandra. Рекомендуємо використовувати Docker версію.

**Передумови:**
- Python 3.7+
- Apache Cassandra (локально на localhost:9042)

**Встановлення залежностей:**
```bash
pip install -r requirements.txt
```

**Запуск лабораторної роботи:**
```bash
python main.py
```

### 📁 Структура файлів

#### Основні файли:
- `main.py` - Головний файл для запуску всіх компонентів
- `cassandra_setup.py` - Налаштування підключення та створення структури БД
- `thermal_power_data_generator.py` - Генератор тестових даних
- `analytical_analysis.py` - Аналітичний модуль (Підваріант А)
- `raw_data_viewer.py` - Модуль для перегляду сирих даних
- `view_raw_data.py` - Інтерактивний переглядач raw data

#### Docker файли:
- `docker-compose.yml` - Основний файл Docker Compose
- `Dockerfile` - Образ для Python додатка
- `Dockerfile.jupyter` - Образ для Jupyter Notebook
- `docker-run.bat` / `docker-run.sh` - Скрипти запуску
- `.dockerignore` - Файли для ігнорування в Docker

#### Документація:
- `README.md` - Основна документація
- `DOCKER_README.md` - Детальна документація Docker версії
- `docker-commands.md` - Команди Docker
- `lab_report_template.md` - Шаблон звіту

#### Додаткові файли:
- `requirements.txt` - Залежності Python
- `cql_init/` - Ініціалізаційні CQL скрипти (автоматичне створення БД)
- `notebooks/` - Jupyter notebooks для аналізу

### 📊 Аналітичний аналіз (Підваріант А)

Модуль `analytical_analysis.py` виконує:

1. **Аналіз виробленої потужності турбін:**
   - Середня, максимальна та мінімальна потужність
   - Рейтинг найефективніших енергоблоків
   - Визначення найкращого блоку

2. **Аналіз викидів забруднюючих речовин:**
   - Статистика викидів CO₂ та NOx
   - Порівняння з екологічними нормативами
   - Оцінка екологічного впливу

3. **Аналіз ефективності станцій:**
   - Порівняння ефективності по станціях
   - Визначення найефективнішої станції
   - Аналіз генерації енергії

4. **Перегляд Raw Data:**
   - Детальний перегляд сирих даних з усіх таблиць
   - Експорт даних у JSON формат
   - Інтерактивний режим перегляду

### 🎯 Основні результати

Програма генерує та аналізує дані, виводячи звіти у форматі:
```
Середня потужність = 200.45 МВт, Максимальна = 220.12 МВт
Найефективніший енергоблок: Block ABC123...
Показує найвищу середню потужність: 205.67 МВт
```

### 🔍 Принципи проєктування

1. **Partition Key** - `block_id` для рівномірного розподілу даних між вузлами
2. **Clustering Key** - `timestamp DESC` для швидкого доступу до останніх даних
3. **Денормалізація** - Дублювання даних для покращення продуктивності запитів
4. **Time-Series Design** - Оптимізація під часові ряди енергетичних даних

### 📈 Переваги Cassandra для енергетичних систем

- **Горизонтальне масштабування** - Легке додавання нових вузлів
- **Висока швидкість запису** - Оптимізація під потокові дані
- **Відмовостійкість** - Автоматична реплікація даних
- **Підтримка часових рядів** - Ефективна робота з timestamp

### 🚀 Додаткові можливості

#### Jupyter Notebook для аналізу:
```bash
# Запуск з Jupyter
docker-compose --profile tools up --build

# Доступ: http://localhost:8888
```

#### CQL Shell для ручної роботи:
```bash
# Доступ до CQL
docker-compose run --rm cqlsh
```

#### Raw Data Viewer для перегляду сирих даних:
```bash
# Інтерактивний перегляд raw data
docker-compose run --rm raw_data_viewer

# Або через основний контейнер
docker-compose exec thermal_power_app python raw_data_viewer.py
```

### 🚀 Масштабування

Система готова для:
- Розширення на більшу кількість станцій
- Збільшення частоти вимірювань
- Додавання нових типів даних
- Інтеграції з реальними IoT-пристроями

### 📝 Логування

Всі операції логуються у файл `thermal_power_lab.log` та виводяться у консоль для моніторингу процесу виконання.

(інші команди та скріншоті наведені в звіті)

------------------------------------------------------------------------

✍️ **Автор:** Наумчик Є.Л., гр. ТР-52мп\
👨‍🏫 **Викладач:** Волков О.В.\
📍 КПІ ім. Ігоря Сікорського, 2025


# ЛАБОРАТОРНА РОБОТА №3
## Оптимізація схем даних в Apache Cassandra для енергетичних часових рядів

## 1. МЕТА РОБОТИ

Спроектувати та оптимізувати схеми даних в Apache Cassandra для зберігання часових рядів телеметрії теплових електростанцій з максимальною продуктивністю запитів.

## 2. КОРОТКИЙ ОПИС ОБРАНОГО ВАРІАНТУ

### 2.1 Характеристика системи
**Варіант 6: Теплові електростанції Східної України**

Система моніторингу теплових електростанцій включає:
- **12 енергоблоків** з різних ТЕС Східної України
- **Курахівська ТЕС** (4 блоки)
- **Вуглегірська ТЕС** (3 блоки) 
- **Слов'янська ТЕС** (3 блоки)
- **Зміївська ТЕС** (2 блоки)

### 2.2 Типи даних
Основні телеметричні показники:
- **Подача палива** (т/год) - 0.5-8.0
- **Тиск пари** (бар) - 40-120
- **Тип палива** (газ/вугілля)
- **Температура пари** (°C) - 400-600
- **Потужність** (МВт) - 0.5-4.0
- **Ефективність** (%) - 15-25

### 2.3 Частота оновлення
- **Дані надходять кожні 15 секунд**
- **4 записи на хвилину**
- **5,760 записів на день на пристрій**
- **172,800 записів на місяць на пристрій**
- **Загалом: 2,073,600 записів за 30 днів**

## 3. ОПИС СХЕМ ДАНИХ

### 3.1 DDL-команди створення таблиць

#### 3.1.1 Схема 1: Simple Wide Row (антипатерн)
```sql
CREATE TABLE thermal_telemetry_simple (
    device_id TEXT,
    timestamp TIMESTAMP,
    power_output DECIMAL,
    temperature DECIMAL,
    pressure DECIMAL,
    fuel_consumption DECIMAL,
    efficiency DECIMAL,
    fuel_type TEXT,
    PRIMARY KEY (device_id, timestamp)
) WITH CLUSTERING ORDER BY (timestamp DESC);
```

#### 3.1.2 Схема 2: Hourly Bucketing (рекомендована)
```sql
CREATE TABLE thermal_telemetry_hourly (
    device_id TEXT,
    bucket_hour TIMESTAMP,
    timestamp TIMESTAMP,
    power_output DECIMAL,
    temperature DECIMAL,
    pressure DECIMAL,
    fuel_consumption DECIMAL,
    efficiency DECIMAL,
    fuel_type TEXT,
    PRIMARY KEY ((device_id, bucket_hour), timestamp)
) WITH CLUSTERING ORDER BY (timestamp DESC);
```

#### 3.1.3 Схема 3: Daily Bucketing + Pre-aggregation
```sql
-- Raw Data Table
CREATE TABLE thermal_telemetry_daily_raw (
    device_id TEXT,
    bucket_date DATE,
    timestamp TIMESTAMP,
    power_output DECIMAL,
    temperature DECIMAL,
    pressure DECIMAL,
    fuel_consumption DECIMAL,
    efficiency DECIMAL,
    fuel_type TEXT,
    PRIMARY KEY ((device_id, bucket_date), timestamp)
) WITH CLUSTERING ORDER BY (timestamp DESC);

-- Aggregates Table
CREATE TABLE thermal_telemetry_hourly_aggregates (
    device_id TEXT,
    hour TIMESTAMP,
    avg_power_output DECIMAL,
    max_power_output DECIMAL,
    min_power_output DECIMAL,
    avg_temperature DECIMAL,
    avg_pressure DECIMAL,
    total_fuel_consumption DECIMAL,
    avg_efficiency DECIMAL,
    fuel_type TEXT,
    record_count INT,
    PRIMARY KEY (device_id, hour)
) WITH CLUSTERING ORDER BY (hour DESC);
```

#### 3.1.4 Альтернативні фільтр-таблиці
```sql
-- High Power Filter Table
CREATE TABLE thermal_high_power_filter (
    device_id TEXT,
    bucket_hour TIMESTAMP,
    timestamp TIMESTAMP,
    power_output DECIMAL,
    temperature DECIMAL,
    pressure DECIMAL,
    PRIMARY KEY ((device_id, bucket_hour), power_output, timestamp)
) WITH CLUSTERING ORDER BY (power_output DESC, timestamp DESC);

-- Low Efficiency Filter Table
CREATE TABLE thermal_low_efficiency_filter (
    device_id TEXT,
    bucket_hour TIMESTAMP,
    timestamp TIMESTAMP,
    efficiency DECIMAL,
    power_output DECIMAL,
    fuel_consumption DECIMAL,
    PRIMARY KEY ((device_id, bucket_hour), efficiency, timestamp)
) WITH CLUSTERING ORDER BY (efficiency ASC, timestamp DESC);
```

### 3.2 Обґрунтування вибору ключів і способу групування

#### 3.2.1 Simple Schema (антипатерн)
- **Partition Key:** `device_id` - всі дані пристрою в одній партиції
- **Clustering Key:** `timestamp` - хронологічний порядок
- **Проблема:** Необмежене зростання партицій

#### 3.2.2 Hourly Bucketing (оптимальна)
- **Partition Key:** `(device_id, bucket_hour)` - розділення по годинах
- **Clustering Key:** `timestamp` - детальний порядок
- **Переваги:** Контрольований розмір партицій, швидкий доступ

#### 3.2.3 Daily Bucketing + Aggregates
- **Raw Data:** `(device_id, bucket_date)` - розділення по днях
- **Aggregates:** `(device_id, hour)` - погодинні агрегати
- **Переваги:** Оптимізація для аналітичних запитів

### 3.3 Схематичне зображення структури даних

```
ТЕПЛОВІ ЕЛЕКТРОСТАНЦІЇ
├─ Курахівська_ТЕС_BLOCK_01
│  ├─ Hourly Buckets: 2025-10-12 14:00:00
│  │  ├─ 14:00:00 → {power: 3.2, temp: 450, pressure: 85}
│  │  ├─ 14:00:15 → {power: 3.4, temp: 455, pressure: 87}
│  │  └─ ...
│  ├─ Hourly Buckets: 2025-10-12 15:00:00
│  └─ ...
├─ Вуглегірська_ТЕС_BLOCK_01
└─ ...

PARTITION STRUCTURE:
├─ Simple: (device_id) → [ALL DATA]
├─ Hourly: (device_id, bucket_hour) → [1 HOUR DATA]
└─ Daily: (device_id, bucket_date) → [1 DAY DATA]
```

## 4. ГЕНЕРАЦІЯ ТЕСТОВИХ ДАНИХ

### 4.1 Опис моделі генератора

#### 4.1.1 Параметри генерації
- **Період:** 30 днів
- **Пристрої:** 12 енергоблоків
- **Частота:** кожні 15 секунд
- **Розрахункові записи:** 2,073,600
- **Фактично згенеровано:** 108,060 записів

#### 4.1.2 Діапазони значень
```python
# Реалістичні діапазони для теплових електростанцій
power_output_range = (0.5, 4.0)  # МВт
temperature_range = (400, 600)    # °C
pressure_range = (40, 120)        # бар
fuel_consumption_range = (0.5, 8.0)  # т/год
efficiency_range = (0.15, 0.25)   # 15-25%
```

#### 4.1.3 Періодичність та залежності
- **Сезонні коливання** - вплив температури навколишнього середовища
- **Тип палива** - газ vs вугілля впливає на ефективність
- **Кореляція параметрів** - підвищення тиску → збільшення потужності

### 4.2 Короткий опис коду

#### 4.2.1 Основні класи
- `ThermalPowerDataGeneratorLab3` - генератор телеметричних даних
- `_generate_realistic_power_curve()` - реалістична крива потужності
- `_generate_related_parameters()` - пов'язані параметри
- `generate_batch_data()` - генерація пакету даних

#### 4.2.2 Особливості реалізації
- **Batch inserts** для пришвидшення запису
- **Реалістичні залежності** між параметрами
- **Врахування типу палива** (газ vs вугілля)
- **Сезонні фактори** та випадкові коливання

## 5. МЕТОДИКА ТЕСТУВАННЯ ПРОДУКТИВНОСТІ

### 5.1 Перелік запитів

#### 5.1.1 Query 1 - Latest Data (очікування < 10 мс)
```sql
SELECT * FROM thermal_telemetry_[schema] 
WHERE device_id = ? 
LIMIT 100
```

#### 5.1.2 Query 2 - Time Range (очікування < 50 мс)
```sql
SELECT * FROM thermal_telemetry_[schema] 
WHERE device_id = ? AND [time_field] >= ? AND [time_field] <= ?
ALLOW FILTERING
```

#### 5.1.3 Query 3 - Daily Aggregation (очікування < 200 мс)
```sql
SELECT * FROM thermal_telemetry_hourly_aggregates 
WHERE device_id = ? AND hour >= ? AND hour <= ?
ALLOW FILTERING
```

#### 5.1.4 Query 4 - Filtered Query (порівняння ALLOW FILTERING і MV)
```sql
-- З ALLOW FILTERING
SELECT * FROM thermal_telemetry_[schema] 
WHERE device_id = ? AND power_output > 2.5
ALLOW FILTERING

-- З Альтернативною фільтр-таблицею
SELECT * FROM thermal_high_power_filter 
WHERE device_id = ? AND bucket_hour = ?
AND power_output > 2.5
ALLOW FILTERING
```

### 5.2 Benchmark-сценарії

- **Кількість ітерацій:** 50-100 для кожного запиту
- **Обсяг даних:** 108,060 записів
- **Умови середовища:** Docker Cassandra 4.1, replication factor = 1
- **Метрики:** average, p50, p95, p99 latency

## 6. РЕЗУЛЬТАТИ ВИМІРЮВАНЬ

### 6.1 Таблиця 1: Порівняння Read Latency (мс)

| Схема | Query Type | Avg Latency | P50 Latency | P95 Latency | P99 Latency |
|-------|------------|-------------|-------------|-------------|-------------|
| Simple | Latest Data | 1.41 | 1.2 | 2.1 | 3.5 |
| Simple | Time Range | 1.41 | 1.2 | 2.0 | 3.2 |
| Simple | Daily Aggregation | 1.39 | 1.1 | 2.0 | 3.1 |
| Simple | Filtered Query | 1.45 | 1.3 | 2.2 | 3.8 |
| Hourly | Latest Data | 1.38 | 1.1 | 2.0 | 3.0 |
| Hourly | Time Range | 1.42 | 1.2 | 2.1 | 3.3 |
| Hourly | Daily Aggregation | 1.40 | 1.2 | 2.0 | 3.2 |
| Hourly | Filtered Query (MV) | 1.88 | 1.5 | 2.8 | 4.5 |
| Daily | Latest Data | 1.31 | 1.0 | 1.9 | 2.8 |
| Daily | Time Range | 1.43 | 1.2 | 2.1 | 3.4 |
| Daily | Daily Aggregation | 1.39 | 1.1 | 2.0 | 3.1 |
| Daily | Filtered Query | 1.44 | 1.3 | 2.1 | 3.6 |

### 6.2 Таблиця 2: Write Throughput

| Схема | Writes/sec | Avg Write Latency (ms) | Batch Size |
|-------|------------|------------------------|------------|
| Simple | 417 | 2.4 | 1000 |
| Hourly | 417 | 2.4 | 1000 |
| Daily | 417 | 2.4 | 1000 |

### 6.3 Таблиця 3: Використання диску

| Схема | Total Size (MB) | Avg Partition Size | Max Partition Size | Compression Ratio |
|-------|----------------|-------------------|-------------------|------------------|
| Simple | 10.00 | 0.977 MB | 0.977 MB | 0.6 (40% економія) |
| Hourly | 10.00 | 0.178 MB | 0.178 MB | 0.6 (40% економія) |
| Daily | 10.00 | 1.068 MB | 1.068 MB | 0.6 (40% економія) |
| Aggregates | 0.01 | 0.001 MB | 0.001 MB | 0.6 (40% економія) |

### 6.4 Таблиця 4: Вплив альтернативних фільтр-таблиць

| Query Type | Without Filter Table (ms) | With Filter Table (ms) | Improvement |
|------------|---------------------------|------------------------|-------------|
| High Power Filter | 1.45 | 1.88 | -30% (повільніше) |
| Low Efficiency Filter | 1.44 | 1.85 | -28% (повільніше) |

**Примітка:** Альтернативні фільтр-таблиці показали гіршу продуктивність через додаткові JOIN операції та ALLOW FILTERING.

## 7. АНАЛІЗ ТА ОПТИМІЗАЦІЯ

### 7.1 Порівняння схем за основними показниками

#### 7.1.1 Read Performance
- **Найкращий:** Daily Schema (1.31 ms для Latest Data)
- **Найгірший:** Hourly Schema з фільтр-таблицями (1.88 ms)

#### 7.1.2 Partition Size Management
- **Найкращий:** Hourly Bucketing (0.178 MB)
- **Найгірший:** Simple Schema (0.977 MB)
- **Рекомендований:** < 10 MB (всі схеми в межах норми)

#### 7.1.3 Write Performance
- **Однаковий:** Всі схеми показали 417 записів/сек
- **Batch Size:** 1000 записів оптимальний для всіх схем

### 7.2 Результати після застосування оптимізацій

#### 7.2.1 Materialized Views
- **Проблема:** Cassandra 4.1 потребує додаткових налаштувань для MV
- **Рішення:** Використання альтернативних фільтр-таблиць
- **Результат:** Гірша продуктивність через додаткові операції

#### 7.2.2 Compression
- **Коефіцієнт стиснення:** 0.6 (40% економія)
- **Алгоритм:** LZ4
- **Ефективність:** Висока для всіх схем

### 7.3 Оцінка ефективності стиснення та розміру партицій

#### 7.3.1 Аналіз розмірів партицій
```
Simple Schema:
- Партицій: 12
- Записів: 48,000
- Середньо записів/партицію: 4,000
- Розмір партиції: 1000.0 KB (0.977 MB)
- Статус: 🟠 Прийнятний (< 10 MB)

Hourly Bucketing:
- Партицій: 72
- Записів: 48,000
- Середньо записів/партицію: 666
- Розмір партиції: 182.29 KB (0.178 MB)
- Статус: 🟠 Прийнятний (< 10 MB)

Daily Bucketing:
- Партицій: 12
- Записів: 48,000
- Середньо записів/партицію: 4,000
- Розмір партиції: 1093.75 KB (1.068 MB)
- Статус: 🟠 Прийнятний (< 10 MB)

Hourly Aggregates:
- Партицій: 12
- Записів: 72
- Середньо записів/партицію: 6
- Розмір партиції: 1.17 KB (0.001 MB)
- Статус: 🟢 Оптимальний (< 10 KB)
```

## 8. РЕКОМЕНДАЦІЇ

### 8.1 Яку схему доцільно використовувати в реальному середовищі

#### 8.1.1 DECISION TREE
```
┌─ Real-time моніторинг (< 10 ms)
│  └─ Hourly Bucketing (рекомендовано)
├─ Аналітичні запити (< 200 ms)
│  └─ Daily Bucketing + Aggregates
├─ Виявлення аномалій
│  └─ Альтернативні фільтр-таблиці
└─ Демонстрація антипатерну
   └─ Simple Schema (НЕ для продакшену)
```

#### 8.1.2 Конкретні рекомендації

**✅ Hourly Bucketing - оптимальний для:**
- IoT-систем з частотою 1-10 записів/хвилину
- Real-time моніторингу енергоблоків
- Систем з критичними затримками < 20 ms

**✅ Daily Bucketing + Aggregates - оптимальний для:**
- Аналітичних систем та звітів
- Довгострокового архівування
- Систем з акцентом на економію простору

**❌ Simple Schema - НЕ рекомендується для:**
- Продакшен систем
- Систем з більше ніж 100K записів на пристрій
- Систем з критичними затримками

**🔧 Альтернативні фільтр-таблиці - використовуйте для:**
- Частих фільтрацій за неключовими полями
- Виявлення аномалій (піки, падіння)
- Оптимізації конкретних запитів

### 8.2 Можливі подальші покращення

#### 8.2.1 Технічні покращення
- **Увімкнути Materialized Views** в cassandra.yaml
- **Налаштувати TimeWindowCompactionStrategy** для часових рядів
- **Додати TTL** для автоматичного видалення старих даних
- **Оптимізувати replication factor** для production

#### 8.2.2 Моніторинг та обслуговування
- **Налаштувати алерти** на розмір партицій
- **Впровадити регулярне очищення** старих даних
- **Моніторинг продуктивності** в реальному часі
- **Резервне копіювання** критичних даних

## 9. ВИСНОВКИ

### 9.1 Загальні результати

#### 9.1.1 Успішні аспекти
- ✅ **Схеми даних створено успішно** - всі 6 таблиць працюють коректно
- ✅ **Генерація даних працює стабільно** - 108,060 записів згенеровано
- ✅ **Hourly Bucketing показав оптимальні результати** - найменший розмір партицій
- ✅ **Daily + Aggregates ефективний для аналітики** - швидкі агрегатні запити
- ✅ **Стиснення дає хорошу економію** - 40% зменшення розміру
- ✅ **Всі схеми в межах норми** - розмір партицій < 10 MB

#### 9.1.2 Виявлені проблеми
- ⚠️ **Materialized Views недоступні** - потребує додаткових налаштувань Cassandra
- ⚠️ **Альтернативні фільтр-таблиці повільніші** - додаткові JOIN операції
- ⚠️ **Simple Schema підтвердив антипатерн** - великі партиції
- ⚠️ **ALLOW FILTERING впливає на продуктивність** - потребує оптимізації

#### 9.1.3 Ключові метрики
- **Загальний час виконання:** 45.63 секунд
- **Загальна кількість записів:** 108,060
- **Створено схем:** 6 (3 основні + 2 фільтр-таблиці + 1 агрегатна)
- **Проведено тестів:** 4 типи запитів
- **Середня економія від стиснення:** 40.0%

### 9.2 Практичні рекомендації для production

#### 9.2.1 Вибір схеми
**Для теплових електростанцій рекомендується:**
1. **Hourly Bucketing** - для real-time моніторингу
2. **Daily + Aggregates** - для аналітичних звітів
3. **Альтернативні фільтр-таблиці** - для специфічних запитів

#### 9.2.2 Оптимізація продуктивності
- Використовуйте **batch inserts** для великих обсягів даних
- Налаштуйте **TTL** для автоматичного очищення
- Моніторьте **розмір партицій** та додавайте алерти
- Використовуйте **TimeWindowCompactionStrategy** для часових рядів

#### 9.2.3 Масштабування
- **Replication factor = 3** для production
- **Моніторинг hot partitions** та балансування навантаження
- **Регулярне очищення** старих даних
- **Резервне копіювання** критичних метрик

### 9.3 Наукова цінність роботи

Проведене дослідження підтвердило теоретичні принципи:
- **Time Bucketing** ефективно контролює розмір партицій
- **Pre-aggregation** значно пришвидшує аналітичні запити
- **Compression** дає значну економію дискового простору
- **Simple Schema** демонструє проблеми масштабування

Результати можуть бути використані для проектування реальних систем моніторингу енергетичних об'єктів.


✍️ **Автор:** Наумчик Є.Л., гр. ТР-52мп\
👨‍🏫 **Викладач:** Волков О.В.\
📍 КПІ ім. Ігоря Сікорського, 2025
